{
  "name": "noa-liff-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --minify --sourcemap --target=es2020",
    "dev": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --sourcemap --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@line/liff": "^2.22.3"
  }
}
